# Frontend Environment Variables Template
# Copy this to a file named .env.production during production configuration, or set it directly in Vercel/Netlify dashboard.

# URL of your deployed Backend API (Express server) ending with /api
VITE_API_URL=https://your-backend-api-on-render.onrender.com/api
